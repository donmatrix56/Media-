<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface" />
    <corners android:radius="16dp" />
    <padding android:left="0dp" android:top="2dp" android:right="0dp" android:bottom="6dp" />
    <size android:height="180dp" android:width="140dp" />
    <!-- Drop shadow should be set via elevation in layout, not here -->
</shape>
